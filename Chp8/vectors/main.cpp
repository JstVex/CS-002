#include "functions.h"

int main() {
//    std::vector<int> v;
//
//    std::vector<int> v2(10); // we can use constructor to initialize the size
//    std::vector<int> v3(10, 99); // another constructor to initialize the size and have default values for elements
//
//    v.push_back(0); // always use push_back to add elements
//
//    for(int i = 1; i < 11; i++)
//        v.push_back(i);
//
//    for(int i = 0; i < v.size(); i++)
//    {
//        std::cout << v[i] << " ";
//    }
//
//    std::cout << sqrt(v[4]);
    run();
    return 0;
}
